#![feature(plugin)]
#![plugin(rocket_codegen)]

extern crate rocket;

pub mod server {

    #[get("/")]
    fn index() -> &'static str {
        "Hello, world!"
    }
    
    pub fn start {
        rocket::ignite().mount("/", routes![index]).launch();
    }
}

